# Upstream: browser-use

## Status: UNVERIFIED

Not yet investigated in this session. Required before Phase 4 (upstream
import) of the bootstrap sequence.

## To determine (evidence required, do not guess)

- Upstream repository: `browser-use/browser-use` (URL to confirm reachable
  and current).
- Pinned revision/commit or release tag to build on.
- Python version and dependency requirements (Playwright, etc.).
- Package boundaries: agent interfaces, browser-control interfaces,
  vision/model interfaces.
- Browser extension compatibility (Chrome/Firefox) — browser-use's default
  distribution model vs. what Ozer needs.
- License terms and required attribution.

## Integration decision (not yet made)

One of: vendor / depend as package / fork / adapter wrapper. Record the
choice and evidence here once Phase 4 is executed, per `AGENTS.md`.

## Divergence log

(empty — no divergence yet, no code imported)
