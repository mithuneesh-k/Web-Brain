{
  "name": "ozer-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Ozer privacy layer — detection, redaction, egress enforcement.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  }
}
