{
  "name": "ozer-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Ozer browser extension — Phase 5 baseline (stub round trip only).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/roundtrip.test.js"
  }
}
